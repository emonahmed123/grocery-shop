const Dashlayout = ({ children,
}: Readonly<{
    children: React.ReactNode;
}>) => {

    return (
        < >


            <h1>dash bord</h1>
            <div>{children}</div>


        </>
    );
};

export default Dashlayout 